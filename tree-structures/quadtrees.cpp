#include <bits/stdc++.h>

using namespace std;


int main(){
    int n;
    cin >> n;
    int l[1024], r[1024]; // 0 == empty, 1 == black, 2 == parent
    while (n--){
        string a, b, c;
        cin >> a >> b;
        
    }
}